import 'package:flutter/material.dart';

class PageViewPopulars extends StatelessWidget {
  const PageViewPopulars({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
